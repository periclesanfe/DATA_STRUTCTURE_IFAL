# Considerando que as operações first apenas retornam o primeiro elemento sem afeta-lo, e dequeue remove o primeiro elemento da fila, implemente uma função que receba uma fila e retorne uma nova fila com os elementos da fila original na ordem inversa.
# E 5 das operações dequeue retornaram excessão, então resta apenas 32 enqueue - 10 dequeue,
# O restou foi 22 enqueue, então a fila está com 22 elementos.
